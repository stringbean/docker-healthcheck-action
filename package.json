{
  "name": "docker-healthcheck-action",
  "version": "1.0.0",
  "description": "GitHub action for checking the health of a Docker container",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts",
    "prettier": "prettier --write .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Stringer",
  "license": "Apache-2.0",
  "dependencies": {
    "@actions/core": "^1.6.0",
    "dockerode": "^4.0.2"
  },
  "devDependencies": {
    "@tsconfig/node20": "^20.1.2",
    "@types/dockerode": "^3.3.1",
    "@types/node": "^20.11.16",
    "@vercel/ncc": "^0.38.1",
    "prettier": "^3.2.5",
    "typescript": "^5.3.3"
  }
}
